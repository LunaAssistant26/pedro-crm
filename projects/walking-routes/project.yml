name: WalkingRoutes
options:
  deploymentTarget:
    iOS: "16.0"
settings:
  base:
    SWIFT_VERSION: "5.0"
    PRODUCT_BUNDLE_IDENTIFIER: com.pedro.WalkingRoutes
targets:
  WalkingRoutes:
    type: application
    platform: iOS
    sources:
      - WalkingRoutes
    info:
      path: WalkingRoutes/Info.plist
    settings:
      base:
        TARGETED_DEVICE_FAMILY: "1,2"
    scheme:
      testTargets: []
